# Example local overrides for Canhoto Digital PWA
# Copy this file to ".env.local" and adjust values for your environment.
# .env.local overrides .env and is NOT committed to VCS by default.

# Base URL of the Django API (local/dev/staging)
VITE_API_BASE_URL=http://localhost:8000

# App environment tag (development|production). Purely informational.
VITE_ENV=development
